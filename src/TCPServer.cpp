#include <sys/socket.h> 
#include <iostream.h>

void main()
{
    // Step 1: Setup the Server Port and Socket
    // Step 2: Wait for incoming connection request
    // Step 3: Read request from the socket
    // Step 4: Reply to socket
    // Step 5: Close the socket
    std::cout << "TCP Server" << std::endl;
}
